@echo off
echo This repository no longer bundles the video agent backend.
echo Run the external video agent service separately, then set AGENT_URL / NEXT_PUBLIC_AGENT_URL.
echo See INTEGRATION.md for the updated integration flow.
exit /b 1
